apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-analytics-pvc
  namespace: workflowhub
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  resources:
    requests:
      storage: 1Gi
