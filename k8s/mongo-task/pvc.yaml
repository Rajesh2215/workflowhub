apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-task-pvc
  namespace: workflowhub
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  volumeName: mongo-task-pv
  resources:
    requests:
      storage: 1Gi
