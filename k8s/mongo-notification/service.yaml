apiVersion: v1
kind: Service
metadata:
  name: mongo-notification
  namespace: workflowhub
spec:
  selector:
    app: mongo-notification
  ports:
    - port: 27017
      targetPort: 27017
  type: ClusterIP
