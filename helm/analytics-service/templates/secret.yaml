apiVersion: v1
kind: Secret
metadata:
  name: {{ include "analytics-service.fullname" . }}-secret
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  RABBITMQ_URL: {{ required "analytics-service.secret.RABBITMQ_URL is required" .Values.secret.RABBITMQ_URL | quote }}
  MONGO_URI: {{ required "analytics-service.secret.MONGO_URI is required" .Values.secret.MONGO_URI | quote }}
