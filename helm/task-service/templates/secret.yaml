apiVersion: v1
kind: Secret
metadata:
  name: {{ include "task-service.fullname" . }}-secret
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  RABBITMQ_URL: {{ required "task-service.secret.RABBITMQ_URL is required" .Values.secret.RABBITMQ_URL | quote }}
  MONGO_URI: {{ required "task-service.secret.MONGO_URI is required" .Values.secret.MONGO_URI | quote }}
